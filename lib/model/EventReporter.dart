import 'package:flutter/material.dart';

class EventReporter {
  String eventStatus;
  int eventCounter;
  Color colorStatus;
  EventReporter(this.eventCounter, this.eventStatus, this.colorStatus);
}
